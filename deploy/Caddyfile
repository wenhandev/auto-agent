{
	email {$ACME_EMAIL:admin@wenhandev.com}
}

{$DOMAIN:rpa.wenhandev.com} {
	encode gzip zstd

	# Public client installers (free static hosting on this VM).
	handle_path /downloads/* {
		root * /srv/downloads
		file_server
	}

	handle {
		reverse_proxy app:8000
	}
}
